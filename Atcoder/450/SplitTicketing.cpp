#include "../bits/stdc++.h"
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)

int main()
{
    int n, a, b, d;

    cin >> n;
    a = 1, b = 3, d = 2;
    vector<vector<int>> c(n - 1, vector<int>(n, 0));
    while (a < n)
    {
        while (d < b)
        {
            if (c[a - 1][d - 1] + c[d - 1][b - 1] <  )
        }
    }
}