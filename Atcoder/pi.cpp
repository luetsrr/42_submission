#include "bits/stdc++.h"
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); ++i)

int main()
{
    double n;

    cin >> n;
    cout << fixed << setprecision(15) << n * n * acos(-1)/ 4 << endl;
    return (0);
}